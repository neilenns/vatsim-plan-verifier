{
  "configurations": [
    {
      "type": "docker",
      "request": "launch",
      "platform": "node",
      "name": "Docker: Launch server",
      "preLaunchTask": "docker-run: server",
      // Important to prevent the container from sticking around and causing failed builds later.
      // https://github.com/microsoft/vscode-docker/issues/3200#issuecomment-916911201
      "removeContainerAfterDebug": true
    }
  ]
}
